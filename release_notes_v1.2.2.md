# LazyFisher 计算器 v1.2.2

## 修复自有船水层字段

- 修复自有船数据把“底层”写成 `bottom`，而计算器内部水层枚举使用 `deep` 的不一致问题。
- 修复路亚搜索遇到底层自有船鱼种时，拟饵、鱼钩和装备参数不显示的问题。
- 修复底钓搜索渲染到自有船底层鱼后中断、导致结果数量看起来变少的问题。
- 统一合并脚本和自有船备份脚本的水层映射。

## 验证

- 计算器数据保持 46 个区域：32 个常规渔场 + 14 个自有船区域。
- 自有船鱼种水层值统一为 `surface / mid / deep`。
- 路亚、底钓、矶竿三种竿型均完成数据渲染路径验证。

## 下载

- LazyFisher计算器-v1.2.2.exe
